type Query {
  getUser(id: Int): User
}

type User {
  id: Int
  email: String
  firstName: String
  lastName: String
  name: String
  role: Int
  avatar: String
  language: String
  company: Int
  teams: Int
}
